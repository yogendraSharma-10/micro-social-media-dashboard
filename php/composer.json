```json
{
    "name": "micro-social-media/backend",
    "description": "PHP backend for the Micro Social Media Dashboard, providing API endpoints for posts, users, follows, and notifications. Designed to be part of a larger interconnected system.",
    "type": "project",
    "license": "MIT",
    "authors": [
        {
            "name": "Your Name",
            "email": "your.email@example.com"
        }
    ],
    "minimum-stability": "stable",
    "require": {
        "php": "^8.1",
        "vlucas/phpdotenv": "^5.6",
        "nikic/fast-route": "^1.3",
        "illuminate/database": "^10.0",
        "firebase/php-jwt": "^6.10",
        "monolog/monolog": "^3.5",
        "symfony/http-foundation": "^6.4",
        "symfony/console": "^6.4",
        "symfony/yaml": "^6.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5",
        "fakerphp/faker": "^1.23"
    },
    "autoload": {
        "psr-4": {
            "App\\": "php/src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "php/tests/"
        }
    },
    "scripts": {
        "post-root-package-install": [
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\""
        ],
        "serve": "php -S localhost:8000 -t php/public/",
        "migrate": "php php/bin/console migrate",
        "seed": "php php/bin/console seed",
        "test": "phpunit --testdox"
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true
    }
}
```